<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login2020">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.register.RegisterActivity"
            android:label="@string/action_register" /> <!-- <activity android:name=".ui.user.UserActivity" -->
        <!-- android:label="@string/user_center"/> -->
        <activity
            android:name=".ui.user.videoInfList"
            android:label="@string/play_list" /> <!-- <activity android:name="io.vov.vitamio.activity.InitActivity" -->
        <!-- android:configChanges="orientation|screenSize|smallestScreenSize|keyboard|keyboardHidden" -->
        <!-- android:launchMode="singleTop" -->
        <!-- android:theme="@android:style/Theme.NoTitleBar" -->
        <!-- android:windowSoftInputMode="stateAlwaysHidden"/> -->
        <activity
            android:name=".ui.user.UserActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/user_center"
            android:screenOrientation="fullSensor" />
        <activity android:name=".controller.getVideoLink"
            android:label="@string/name_lists"/>
    </application>

</manifest>